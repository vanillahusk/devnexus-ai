global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: dynamic-tp-demo
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - dynamic-tp-demo:8080