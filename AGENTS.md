# DevNexus AI 协作入口

本文件是项目级 AI 协作入口。`.agents/` 保存按任务类型拆分的详细规则，执行任务时必须通过本文件显式读取。

## 规则优先级

- 用户当次明确要求优先于项目默认流程；系统和开发者约束始终优先。
- 子目录若存在更近的 `AGENTS.md`，局部规则优先于本文件。
- `.agents/` 只保存稳定的工作方法和质量约束，不保存个人材料或任务进度。

## 工作前检查

在分析、修改文件或运行命令前：

1. 完整阅读 `.agents/README.md`。
2. 根据任务类型完整阅读对应规则；一个任务可以命中多个文件：
   - 命令、服务、容器或压测：`.agents/resource-boundaries.md` 与 `.agents/testing-workflow.md`
   - RAG、模型、向量检索或 Agent：`.agents/rag-workflow.md`
   - RocketMQ、Outbox、幂等、重试或一致性：`.agents/backend-reliability.md`
   - 任意文件修改或交付：`.agents/delivery-checklist.md`
3. 检查当前工作区状态，保护已有且不属于本任务的改动。
4. 用户要求不明确时，先检查仓库现状和相关文档，不凭文件名猜测完成状态。

## 不可破坏的约束

- 不为展示技术而堆砌组件；新增技术必须解决明确问题。
- 文件存在、编译成功或 Mock 测试通过，不等于真实依赖链路已经验证。
- 用户要求暂停、停止或“不启动”时，不得启动新进程、容器或服务。
- 当前消息主线统一为 RocketMQ，不恢复 RabbitMQ 双轨实现。
- 不提交 API Key、Token、密码、个人数据、运行日志或生成报告。

## 交付原则

- 可靠性改动应同时考虑正常、失败、恢复、幂等和可观测性。
- 最终说明实际改动、验证结果、未验证边界和运行中服务状态。
- 结论必须与证据等级匹配，不使用“绝对不重复”“完全可用”等无法证明的表述。
